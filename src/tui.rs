/// `app` is the main application logic and structure
pub mod app;

/// `tui` contains TUI specific logic, including setup and teardown
/// Heavily based on the tutorials from Ratatui
pub mod tui;

/// `ui` contains the layout and formatting code for the TUI
pub mod ui;
